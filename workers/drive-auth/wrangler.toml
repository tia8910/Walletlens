name = "walletlens-drive-auth"
main = "index.js"
compatibility_date = "2026-09-02"

# This worker holds no storage — it is a stateless proxy that swaps auth
# codes and refresh tokens for fresh Google access tokens.
# The client secret must be set once:
#   wrangler secret put GOOGLE_CLIENT_SECRET
